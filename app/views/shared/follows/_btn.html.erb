<% if current_user != followed_user %>
  <% if current_user.folloing?(user) %>
    <%= link_to 'フォローしない', users_destroy_follow(followed_user.id), method: :delete %>
  <% else %>
    <%= link_to 'フォロ-する', users_create_follow(followed_user.id), method: :post %>
  <% end %>
<% end %>
